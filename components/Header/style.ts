import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  container: {
    flexDirection: 'row',
    justifyContent: 'flex-end',
    alignItems: 'center',
  },
  button: {borderRadius: 5, justifyContent: 'center', alignItems: 'center' },
});

export default styles;
